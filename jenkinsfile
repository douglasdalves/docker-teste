pipeline {
    agent { 
        dockerfile {
            filename 'Dockerfile'
            args '-v /.cache/ -v /.bower/  -v /.config/configstore/'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
